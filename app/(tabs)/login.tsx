import React from 'react';
import { Text, View } from 'react-native';

const loginScreen = () => {
  return (
    <View style={{
        alignItems: "center",
        display: "flex",

        
    }}>
      <Text>login Page this is</Text>
    </View>
  )
}

export default loginScreen;